# --- Admin Nextcloud (créé au 1er démarrage si volume vierge)
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=Passw0rd!

# --- Domaines de confiance (trusted domains)
# Mettre exactement ce par quoi on accède (IP/nom). Les valeurs avec espaces doivent être "entre guillemets".
NEXTCLOUD_TRUSTED_DOMAINS="10.0.11.1 10.0.11.11 10.0.11.254 localhost 127.0.0.1"

# --- Base de données
MYSQL_DATABASE=nextcloud
MYSQL_USER=nextcloud
MYSQL_HOST=nc-mariadb
MYSQL_PORT=3306

# --- Publication du port 
PUBLISHED_PORT=8080